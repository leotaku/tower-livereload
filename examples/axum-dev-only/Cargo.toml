[package]
name = "axum-dev-only"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.6.18"
tokio = { version = "1.28.2", features = ["macros", "rt-multi-thread"] }
tower-livereload = { path = "../.." }
