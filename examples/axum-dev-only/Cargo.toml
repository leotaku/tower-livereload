[package]
name = "axum-dev-only"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.7"
tokio = { version = "1.40.0", features = ["macros", "rt-multi-thread"] }
tower-livereload = { path = "../.." }
