[package]
name = "axum-dev-only"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.8"
tokio = { version = "1.48.0", features = ["macros", "rt-multi-thread"] }
tower-livereload = { path = "../.." }
